{
  "name": "multi-sync-planner",
  "version": "1.0.0",
  "description": "Multi-platform synchronized planner with Cloudflare deployment",
  "main": "index.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "vitest"
  },
  "keywords": ["planner", "scheduler", "sync", "cloudflare"],
  "author": "AI Assistant",
  "license": "MIT",
  "dependencies": {
    "@cloudflare/workers-types": "^4.20231218.0",
    "itty-router": "^3.0.12",
    "zod": "^3.22.4"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.1.0",
    "vitest": "^1.3.0",
    "wrangler": "^3.22.1"
  }
}